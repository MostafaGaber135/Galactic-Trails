class PlanetInfoModel {
  final String title, subTitle, sunnyTitle, distanceTitle, image;

  const PlanetInfoModel({
    required this.title,
    required this.subTitle,
    required this.sunnyTitle,
    required this.distanceTitle,
    required this.image,
  });
}
